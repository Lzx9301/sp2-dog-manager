rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 私人系統：只有登入的使用者（你自己）能讀寫任何資料
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
